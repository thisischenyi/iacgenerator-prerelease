# 修复 README 资源重复问题

## 🐛 问题描述

**现象：**
README.md 显示了重复的资源统计，且格式不一致，例如：
```markdown
- 1 x aws_s3
- 1 x aws s3
```

**原因分析：**
1.  **资源类型不规范**：`InputParser` 提取的资源类型可能包含空格（如 "aws s3"），而 `InformationCollector` 有时会规范化为 "aws_s3"，有时直接透传。导致同一个资源类型存在多种变体。
2.  **README 生成逻辑重复**：`TerraformCodeGenerator._generate_readme` 中存在重复的代码块，导致每个资源类型被统计两次。
3.  **统计逻辑未归一化**：在生成 README 时，未将 "aws s3" 和 "aws_s3" 视为同一种类型。

## ✅ 修复方案

### 1. 统一资源类型规范化

在所有涉及资源类型处理的地方，统一使用 `normalized_type = raw_type.lower().replace(" ", "_")` 逻辑。

**修改的文件：** `backend/app/agents/nodes.py`

*   **InputParser**: 在提取资源后立即规范化 `type` 字段。
*   **InformationCollector**:
    *   在合并新旧资源时，使用规范化类型进行匹配。
    *   在最终输出前，再次检查并规范化所有资源的 `type` 和 `resource_type`。

### 2. 修复 README 生成逻辑

**修改的文件：** `backend/app/services/terraform_generator.py`

*   **删除重复代码**：移除了 `_generate_readme` 中重复的循环写入逻辑。
*   **增强归一化**：在统计资源数量时，同样应用 `.replace(" ", "_")` 归一化逻辑，确保 "aws s3" 和 "aws_s3" 被统计在一起。

## 🧪 验证结果

创建了集成测试 `test_readme_fix_integration.py` 验证全流程：

1.  **InputParser 测试**：输入 "aws s3"，输出 `type: "aws_s3"`。✅
2.  **InformationCollector 测试**：透传资源，保持 `type: "aws_s3"`。✅
3.  **Generator 测试**：
    *   输入混合数据 `[{"resource_type": "aws_s3"}, {"resource_type": "aws s3"}]`
    *   输出 README：`- 2 x aws_s3` ✅

## 📝 现在的行为

用户输入 "Create an aws s3 bucket" -> 系统内部统一为 `aws_s3` -> 生成的 README 显示：
```markdown
## Resources
This configuration will create **1** resource:
- 1 x aws_s3
```

问题已彻底解决。
